
  # MJV PL Prototype - WEB

  This is a code bundle for MJV PL Prototype - WEB. The original project is available at https://www.figma.com/design/NSlAE0cu9qAvIBCKd3CzFd/MJV-PL-Prototype---WEB.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  